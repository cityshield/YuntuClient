# YuntuClient 配置文件示例
#
# 使用说明：
# 1. 复制此文件为 config.ini
# 2. 根据实际情况修改配置项
# 3. config.ini 不会被提交到 Git

[Server]
# 服务器地址（必填）
# 开发环境示例: http://localhost:8000
# 生产环境示例: https://api.yuntucv.com
url=http://localhost:8000

# API 版本前缀（一般不需要修改）
api_prefix=/api/v1

[Client]
# 客户端日志级别: debug, info, warning, error
log_level=info

# 日志文件保留天数
log_retention_days=7

# 是否自动上传日志到 OSS
auto_upload_logs=true

# 日志上传间隔（分钟）
log_upload_interval=30

[Application]
# 应用名称
app_name=YuntuClient

# 应用版本（由程序自动管理，通常不需要修改）
# app_version=1.0.0
